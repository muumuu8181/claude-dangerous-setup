# 🛡️ セキュリティガイド

Claude Code Safe Dangerous Mode Setup のセキュリティについて詳しく説明します。

## 🎯 セキュリティ設計原則

### 1. 多層防護 (Defense in Depth)
- **レイヤー1**: proot隔離環境によるシステム分離
- **レイヤー2**: 権限ベース制御による危険コマンドブロック
- **レイヤー3**: 自動バックアップによるデータ保護
- **レイヤー4**: セッション制限による暴走防止

### 2. 最小権限の原則 (Principle of Least Privilege)
必要最小限の権限のみを付与し、危険な操作を事前にブロック

### 3. セキュアバイデフォルト (Secure by Default)
初期設定から安全な設定で動作開始

## 🔒 実装されているセキュリティ機能

### proot隔離環境
```bash
# システムディレクトリへの影響を完全遮断
proot --kill-on-exit --mount=/system --mount=/vendor
```

**効果:**
- ルートファイルシステムの変更不可
- システム設定の改変防止
- プロセス暴走時の自動終了

### 権限制御システム

#### 許可されるコマンド
```json
{
  "allow": [
    "Edit",           // ファイル編集
    "Write",          // ファイル書き込み
    "Read",           // ファイル読み取り
    "Bash(npm run *)", // npm スクリプト実行
    "Bash(node *)",   // Node.js 実行
    "Bash(python *)", // Python 実行
    "Bash(git *)",    // Git 操作
    "Bash(ls *)",     // ディレクトリ一覧
    "Bash(mkdir *)",  // ディレクトリ作成
    "Bash(cp *)",     // ファイルコピー
    "Bash(mv *)"      // ファイル移動
  ]
}
```

#### 禁止されるコマンド
```json
{
  "deny": [
    "Bash(rm -rf *)",    // 再帰的削除
    "Bash(sudo *)",      // 管理者権限実行
    "Bash(su *)",        // ユーザー切り替え
    "Bash(curl *)",      // 外部通信
    "Bash(wget *)",      // ファイルダウンロード
    "Bash(termux-* *)",  // Termux固有コマンド
    "Bash(pkg *)",       // パッケージ管理
    "Bash(apt *)"        // パッケージ管理
  ]
}
```

### 自動バックアップシステム
```bash
# 作業開始前に自動でタイムスタンプ付きバックアップ
BACKUP_DIR="$SANDBOX_DIR/backups/$(date +%Y%m%d-%H%M%S)"
rsync -av --exclude='node_modules' --exclude='.git' "$(pwd)/" "$BACKUP_DIR/"
```

**特徴:**
- node_modules, .git を除外して効率化
- タイムスタンプでバージョン管理
- 一発復元コマンド (`restore-backup`)

## 🚨 リスクアセスメント

### 高リスク要因とその対策

| リスク | 影響度 | 発生確率 | 対策 |
|--------|--------|----------|------|
| システムファイル破損 | 高 | 低 | proot隔離環境で完全遮断 |
| プロジェクトデータ消失 | 高 | 中 | 自動バックアップ + 復元機能 |
| 外部への情報漏洩 | 中 | 低 | ネットワークコマンド禁止 |
| プロセス暴走 | 中 | 中 | セッション制限 + kill-on-exit |

### 残存リスク

**中リスク:**
- プロジェクト内でのファイル操作ミス
- 設定ファイルの誤編集

**低リスク:**
- バックアップ容量の増大
- パフォーマンスの軽微な低下

## 🔍 セキュリティ監査

### ログ記録
```bash
# 全ての操作をタイムスタンプ付きで記録
echo "$(date '+%H:%M') $OPERATION" >> "$SETUP_LOG"
```

### 定期的なセキュリティチェック

```bash
# 権限設定の確認
cat ~/.config/claude/settings.json | jq '.permissions'

# proot環境の確認
proot --version

# バックアップの確認
ls -la ~/claude-sandbox/backups/
```

## 📊 セキュリティメトリクス

### 測定可能な指標

- **隔離効果**: システムディレクトリへのアクセス試行 → 100%ブロック
- **バックアップ成功率**: 作業開始時のバックアップ作成 → 99%+
- **権限制御効果**: 危険コマンドブロック率 → 100%
- **復旧時間**: バックアップからの復元時間 → 30秒以内

## 🎓 セキュリティベストプラクティス

### 使用前チェックリスト

- [ ] Termux環境で実行されていることを確認
- [ ] prootパッケージがインストール済み
- [ ] 権限設定ファイルが正しく配置されている
- [ ] バックアップディレクトリに十分な容量がある

### 定期メンテナンス

```bash
# 週次: 古いバックアップの削除
find ~/claude-sandbox/backups -name "*" -mtime +7 -exec rm -rf {} \;

# 月次: 権限設定の見直し
vim ~/.config/claude/settings.json

# 月次: ログファイルのローテーション
logrotate ~/claude-sandbox/logs/*.log
```

## 🚨 インシデント対応

### セキュリティインシデント発生時

1. **即座の対応**
   ```bash
   # Claude Code プロセスを即座に終了
   pkill -f "claude"
   
   # 最新バックアップから復元
   restore-backup
   ```

2. **調査と分析**
   ```bash
   # ログファイルの確認
   tail -100 ~/work-history/claude-*.log
   
   # 変更されたファイルの確認
   git status
   git diff
   ```

3. **報告と改善**
   - GitHub Issues にて報告
   - 再発防止策の検討
   - 設定の見直し

## 📞 セキュリティサポート

### 緊急時連絡先
- **GitHub Issues**: https://github.com/muumuu8181/claude-dangerous-setup/issues
- **セキュリティラベル付きで報告してください**

### セキュリティ更新
- 重要なセキュリティ更新は GitHub Releases でアナウンス
- 自動更新チェッカーは計画中

## ⚖️ 責任範囲

### 提供される保護
- ✅ システムレベルの隔離
- ✅ 基本的な権限制御
- ✅ データバックアップ機能
- ✅ プロセス制御

### ユーザーの責任
- ❗ 重要データの別途バックアップ
- ❗ 定期的な設定見直し
- ❗ セキュリティ更新の適用
- ❗ 適切な使用環境の維持

---

**セキュリティに関する質問や懸念がある場合は、遠慮なくGitHub Issuesまでご連絡ください。**